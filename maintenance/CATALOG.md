# 文章目录与发布检查

`data/articles.json` 是文章展示信息的唯一编辑入口：中文标题、原题、主题、搜索别名、内容类型、来源、版本说明、验证状态。编号沿用原有文章，路由保持 `#/docs/编号`。

`maintenance/upstream-sources.json` 保留为上一次上游同步的审计记录，不作为前端目录。更新上游内容时，同步更新文章元数据中的来源与核对日期。

## 更新方式

1. 新增或修改 `docs/编号.md` 正文。原始 H1 保留用于溯源和旧锚点；网页显示中文标题。
2. 更新 `data/articles.json` 的对应记录。主题的第一个值决定侧栏位置，其他主题用于交叉收录。推荐路径仅填写文章 ID。
3. 首页文字在 `maintenance/home-template.md` 编辑，推荐文章 ID 在元数据的 `featured` 中编辑。
4. 执行 `node maintenance/build-catalog.mjs`。自动生成 README、侧栏、专题、路径、会议页和搜索索引；这些生成文件需要一起提交。
5. 执行 `node --test maintenance/test-catalog.cjs` 和 `git diff --check`。

生成工具也会更新 `index.html` 中本地脚本和样式的内容哈希，避免发布后继续使用旧版资源。修改这些脚本或样式后也应重新运行生成工具。`index.html` 的其余配置仍可直接编辑。

生成工具仅依赖 Node.js 标准库，网站访问不需要 Node.js 或额外服务。网站的全文索引在首次输入关键词时加载；失败时保留标题与别名搜索，显示重试入口。搜索内容不发送给外部搜索服务。

## 验证信息

- `tested` 必须提供现存验证记录、日期和实际覆盖范围。不能仅凭原文描述或同步成功标成实测。
- `not-recorded` 表示没有独立运行记录。同步日期仅说明内容核对，不说明兼容所有版本。
- 不根据文章中偶然出现的版本号推断最低适用版本；版本说明应人工核对后填写。

## 发布验收

推送前检查：根地址及 `#/README` 均直接显示首页；首页搜索、6 个问题入口、专题、路径和会议页可用；100 个编号文档都能打开；表格横向滚动、中文标题、目录定位、浏览器前进后退、移动端菜单和暗色模式正常。

可选浏览器回归：安装 Playwright 及 Chromium 后，执行 `node maintenance/test-site.cjs`。默认连接本机 4175 端口；可用 `HOWTO_URL` 指定预览地址，`PLAYWRIGHT_PACKAGE` 指定已有安装，`CHROME_PATH` 指定本机 Chrome 可执行文件。测试采用独立浏览器配置，并屏蔽统计与评论请求。

提交后仍需待 GitHub Pages 完成部署，在实际域名复查首页、搜索索引版本与代表文章。静态检查通过不等于线上部署已完成；本地验收后不自动推送。
# 收录日期

首页“最近收录”使用文章的 `addedOn`（YYYY-MM-DD），按日期倒序排列，同日按编号倒序。历史日期取 Git 首次新增该文章的日期，不使用会议日期或后续校订日期；新增精选项必须补齐此字段。
